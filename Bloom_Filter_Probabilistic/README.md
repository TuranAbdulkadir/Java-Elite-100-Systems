# Bloom_Filter_Probabilistic

## Project Overview
An implementation of a **Bloom Filter**. It tells if an element **possibly exists** or **definitely does not exist** using bit arrays and hashing, saving massive memory in databases.

## Tech Stack
* **Structure:** BitSet
* **Logic:** Multiple Hashing