# Registry_Cleaner_Tool

## Project Overview
A system utility that parses a simulated **Windows Registry** (Hierarchical Key-Value database). It scans for "broken keys" (files that no longer exist) and cleans them to optimize system performance.

## Structure
* **HKEY_LOCAL_MACHINE:** Root
* **Logic:** Recursive Scan