# File_System_Inode_Structure

## Project Overview
A simulation of a Unix-like File System using **Inodes**. It maps filenames to Inode numbers, which then point to the actual data blocks on the disk structure.

## Structure
* **Inode:** Metadata (Size, Permissions)
* **Directory:** Name -> Inode Mapping