# Bios_Boot_Sequence

## Project Overview
A simulation of the **BIOS Power-On Self-Test (POST)**. It performs hardware checks (RAM, CPU, Keyboard) before handing over control to the Operating System Bootloader (MBR).

## Steps
* **POST:** Hardware Check
* **Boot:** Load Kernel