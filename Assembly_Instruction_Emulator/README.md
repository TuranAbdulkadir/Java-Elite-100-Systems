# Assembly_Instruction_Emulator

## Project Overview
A simplified **CPU Emulator** that executes Assembly-like instructions (MOV, ADD, SUB). It manipulates virtual registers (AX, BX) and flags, mimicking low-level hardware logic.

## Instructions
* **MOV:** Move data
* **ADD:** Arithmetic add