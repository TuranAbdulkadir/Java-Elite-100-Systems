# Mathematical_Expression_Evaluator

## Project Overview
A parser that evaluates mathematical strings like `(10 + 20) * 2`. It uses the **Shunting Yard Algorithm** or recursive descent to handle operator precedence and parentheses correctly.

## Logic
* **Logic:** Java ScriptEngine (Built-in)
* **Eval:** Runtime Calculation