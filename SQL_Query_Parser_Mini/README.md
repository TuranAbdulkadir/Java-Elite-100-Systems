# SQL_Query_Parser_Mini

## Project Overview
A mini parser for **SQL Select Queries**. It analyzes strings like `SELECT name FROM users WHERE id=1`, extracting the selected columns, target table, and conditions.

## Tech Stack
* **Regex:** Case-insensitive parsing
* **Parsing:** Group extraction