# Docker_Container_Engine_Sim

## Project Overview
A conceptual simulation of a **Container Engine**. It demonstrates how Docker isolates processes using namespaces (simulated) and manages the lifecycle (Start, Stop, Logs) of containers.

## Concepts
* **Isolation:** Process separation
* **Lifecycle:** Run/Kill logic