# Abstract_Syntax_Tree_Builder

## Project Overview
An example of building an **Abstract Syntax Tree (AST)** for a simple expression `1 + 2 * 3`. Compilers use ASTs to understand the structure of code before generating machine code.

## Structure
* **Node:** Expr, Number, BinaryOp
* **Traversal:** Recursive