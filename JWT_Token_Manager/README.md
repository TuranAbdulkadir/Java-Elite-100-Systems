# JWT_Token_Manager

## Project Overview
A lightweight **JSON Web Token (JWT)** implementation. It generates signed tokens containing user claims (payload) and verifies them using a secret key, standard in modern stateless authentication.

## Structure
* **Header:** Algo type
* **Payload:** User Data
* **Signature:** HMACSHA256