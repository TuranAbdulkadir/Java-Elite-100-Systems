# JSON_Parser_Custom

## Project Overview
A custom **JSON Parser**. It takes a raw JSON string and parses it into a Java Map structure by extracting keys and values, simulating how libraries like Jackson or Gson work under the hood.

## Logic
* **Format:** Key-Value Pairs
* **Parsing:** String Manipulation