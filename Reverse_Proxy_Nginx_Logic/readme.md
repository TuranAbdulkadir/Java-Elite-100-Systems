# Reverse_Proxy_Nginx_Logic

## Project Overview
A simulation of a **Reverse Proxy**. It receives client requests and forwards them to backend servers, hiding the internal infrastructure and providing an additional layer of security and load balancing.

## Logic
* **Proxying:** Client -> Proxy -> Server
* **Anonymity:** Hides backend IP