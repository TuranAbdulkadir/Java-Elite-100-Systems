# Regex_Engine_From_Scratch

## Project Overview
A simplified **Regular Expression Engine**. It implements basic pattern matching logic (supporting `.` for any char and `*` for repetition) using recursion, without using Java's built-in `Pattern` class.

## Logic
* **Algorithm:** Recursive Backtracking
* **Symbols:** `.` (Wildcard), `*` (Star)