# Network_Port_Scanner

## Project Overview
A multi-threaded **Port Scanner**. It attempts to connect to a range of TCP ports on a target IP address to identify running services (like HTTP, SSH, FTP), similar to **Nmap**.

## Tech Stack
* **Networking:** Sockets
* **Concurrency:** ExecutorService (ThreadPool)