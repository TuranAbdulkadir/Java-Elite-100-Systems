# Semaphore_Mutex_Implementation

## Project Overview
An implementation of a **Mutex (Mutual Exclusion)** using Semaphores. It ensures that only one thread can access a critical section (shared resource) at a time to prevent Race Conditions.

## Concept
* **Lock:** Acquire / Release
* **Safety:** Thread Synchronization