# LRU_Cache_Implementation

## Project Overview
A **Least Recently Used (LRU) Cache**. It stores a limited number of items and automatically discards the least recently accessed item when the cache is full. Essential for optimizing system performance.

## Tech Stack
* **Structure:** HashMap + Doubly Linked List
* **Complexity:** O(1) for Get and Put