# API_Gateway_Router

## Project Overview
An **API Gateway** logic implementation. It acts as a single entry point for clients, routing requests (e.g., `/users`, `/orders`) to the appropriate microservices behind the scenes.

## Logic
* **Routing:** Path-based forwarding
* **Security:** Centralized Auth check