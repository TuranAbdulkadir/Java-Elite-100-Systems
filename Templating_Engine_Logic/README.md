# Templating_Engine_Logic

## Project Overview
A custom **Templating Engine**. It processes a text template containing variables like `{{name}}` and replaces them with actual data from a context map, similar to **Handlebars** or **Jinja2**.

## Logic
* **Syntax:** {{ variable }}
* **Process:** String Replacement