# Markdown_To_HTML_Converter

## Project Overview
A transpiler that converts **Markdown** syntax (like `# Heading`, `**bold**`) into valid **HTML** tags. This demonstrates text processing and syntax replacement logic used in static site generators.

## Tech Stack
* **Regex:** Pattern Replacement
* **Target:** HTML5