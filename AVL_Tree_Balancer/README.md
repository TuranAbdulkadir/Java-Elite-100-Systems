# AVL_Tree_Balancer

## Project Overview
An implementation of a self-balancing **Binary Search Tree (AVL Tree)**. It automatically performs rotations (Left/Right) to ensure the tree height remains logarithmic, guaranteeing O(log n) search speeds.

## Tech Stack
* **Logic:** Tree Rotations
* **Property:** Height Balance Factor