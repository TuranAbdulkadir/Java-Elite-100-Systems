# Shell_Command_Interpreter

## Project Overview
A custom **Command Line Interface (Shell)**. It parses user input strings, identifies commands (like `ls`, `echo`, `exit`), and executes the corresponding functions in an infinite loop.

## Commands
* **echo:** Print text
* **ls:** List files (simulated)