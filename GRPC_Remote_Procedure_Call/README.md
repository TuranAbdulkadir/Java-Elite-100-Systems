# GRPC_Remote_Procedure_Call

## Project Overview
A simulation of **gRPC (Remote Procedure Call)**. Unlike REST API which sends JSON text, this simulates calling a function on a remote server directly, as if it were a local method.

## Logic
* **Protocol:** RPC
* **Format:** Binary (Simulated)    